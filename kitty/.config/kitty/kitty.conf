# Basic options --------------------------------------------------------------

cursor_blink_interval 0
enable_audio_bell no

# Replace clipboard at each copy (solved trouble with tmux);
clipboard_control write-primary write-clipboard no-append

hide_window_decorations true

background_opacity 0.99

 Remove remap ---------------------------------------------------------------

clear_all_shortcuts yes

# These are special and need to be disable unitary
map ctrl+shift+r discard_event
map ctrl+shift+t no_op

# Keep this remap
map ctrl+shift+c copy_to_clipboard
map ctrl+shift+v paste_from_clipboard
map ctrl+v pass_through

# Add remap ------------------------------------------------------------------

map ctrl+plus change_font_size all +2.0
map ctrl+minus change_font_size all -2.0

# Permet d'utiliser Alt+[ en binding
# Par défaut Alt+[ envoie \e[ qui est le prefixe CSI (Control Sequence Introducer)
# Cela casse la plupart des TUI (opencode, lazygit, ...)
map alt+[ send_text all \x1b[777~

# Permet d'utiliser Ctrl+[ en binding
# Par défaut cette touche n'est pas reconnu
# Elle est très utile pour opencode
map ctrl+enter send_text all \x1b[13;5u


# Appearence -----------------------------------------------------------------

# Nerd font
symbol_map U+e000-U+e00a,U+ea60-U+ebeb,U+e0a0-U+e0c8,U+e0ca,U+e0cc-U+e0d7,U+e200-U+e2a9,U+e300-U+e3e3,U+e5fa-U+e6b7,U+e700-U+e8ef,U+ed00-U+efc1,U+f000-U+f2ff,U+f000-U+f2e0,U+f300-U+f381,U+f400-U+f533,U+f0001-U+f1af0 Symbols Nerd Font Mono

shell_integration no-title
