bind -n 'C-h' if-shell "~/.local/bin/tmux-is-vim" 'send-keys C-h'  'select-pane -L'
bind -n 'C-j' if-shell "~/.local/bin/tmux-is-vim" 'send-keys C-j'  'select-pane -D'
bind -n 'C-k' if-shell "~/.local/bin/tmux-is-vim" 'send-keys C-k'  'select-pane -U'
bind -n 'C-l' if-shell "~/.local/bin/tmux-is-vim" 'send-keys C-l'  'select-pane -R'
bind -T copy-mode-vi 'C-h' select-pane -L
bind -T copy-mode-vi 'C-j' select-pane -D
bind -T copy-mode-vi 'C-k' select-pane -U
bind -T copy-mode-vi 'C-l' select-pane -R
bind -T copy-mode-vi 'C-\' select-pane -l

bind -n 'M-h' if-shell "~/.local/bin/tmux-is-vim" 'send-keys M-h'  'select-pane -L'
bind -n 'M-j' if-shell "~/.local/bin/tmux-is-vim" 'send-keys M-j'  'select-pane -D'
bind -n 'M-k' if-shell "~/.local/bin/tmux-is-vim" 'send-keys M-k'  'select-pane -U'
bind -n 'M-l' if-shell "~/.local/bin/tmux-is-vim" 'send-keys M-l'  'select-pane -R'
bind -T copy-mode-vi 'M-h' select-pane -L
bind -T copy-mode-vi 'M-j' select-pane -D
bind -T copy-mode-vi 'M-k' select-pane -U
bind -T copy-mode-vi 'M-l' select-pane -R
bind -T copy-mode-vi 'M-\' select-pane -l
