#!/bin/bash

variant=""
read -r variant 2>/dev/null <"${XDG_STATE_HOME:-${HOME}/.local/state}/theme"

case ${variant} in
dark | light)
	printf 'gruvbox-%s\n' "${variant}"
	;;
*)
	printf '%s\n' "${BAT_THEME:-gruvbox-light}"
	;;
esac
